name: Release

on:
  push:
    branches:
      - main

jobs:
  release:
    uses: octo-devops-trainning/github-actions/.github/workflows/terraform-release.yml@v1.0.0
    permissions:
      contents: write
      pull-requests: read
    secrets: inherit
